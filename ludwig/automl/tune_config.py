"""
tune_config.py

(1) tune batch size to fit hardware constraints
(2) tune learning rate
(3) tune hyperopt search spaces based on dataset (maybe)
"""


from typing import Dict


def tune_batch_size(model_config: Dict):
    # TODO (ASN)
    return model_config


def tune_learning_rate(model_config: Dict):
    # TODO (ASN)
    return model_config
