# Register all preprocessors
from ludwig.schema.features.preprocessing import (audio, bag, binary,  # noqa
                                                  category, date, h3, image,
                                                  number, sequence, set, text,
                                                  timeseries, vector)
