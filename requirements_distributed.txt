# requirements for dask
dask[dataframe]<2023.4.0
pyarrow

# requirements for ray
ray[default,data,serve,tune]>=2.2.0,<2.5
tensorboardX<2.3
GPUtil
tblib
awscli

# TODO: replace with deepspeed 0.10.1 when released
git+https://github.com/microsoft/DeepSpeed.git@8a63754

# requirements for daft
getdaft[ray]
